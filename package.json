{
  "name": "after-html5",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:generate": "node scripts/generate-docs.mjs",
    "docs:dev": "npm run docs:generate && vitepress dev docs --host 127.0.0.1",
    "docs:build": "npm run docs:generate && vitepress build docs",
    "docs:preview": "vitepress preview docs --host 127.0.0.1"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "vue": "^3.5.17"
  }
}
